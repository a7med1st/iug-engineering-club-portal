import { requireAdmin } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { createActivity, createMember, deleteActivity, saveGuide, addStructureItem } from "./actions";
export const dynamic="force-dynamic";
export default async function Admin(){await requireAdmin();const [departments,activities,members]=await Promise.all([prisma.department.findMany({include:{guide:true},orderBy:{sortOrder:"asc"}}),prisma.activity.findMany({include:{department:true},orderBy:{startsAt:"desc"},take:12}),prisma.user.findMany({where:{role:"MEMBER"},include:{department:true},orderBy:{createdAt:"desc"}})]);return <div className="admin-wrap"><aside className="admin-side"><h2>لوحة الإدارة</h2><nav><a href="#activities">الأنشطة</a><a href="#members">حسابات الأعضاء</a><a href="#guides">أدلة الأقسام</a><a href="#structure">هيكلية النادي</a></nav></aside><section className="admin-main"><h1>لوحة إدارة النادي الهندسي</h1><p className="muted">هذه الصفحة لا يمكن الوصول إليها من حساب الطالب أو العضو العادي.</p><div className="admin-grid"><div className="admin-card" id="activities"><h2>إضافة نشاط</h2><form action={createActivity} className="stack-form"><label>اسم النشاط<input name="title" required/></label><label>وصف مختصر<textarea name="description" required/></label><label>المكان<input name="location" required/></label><label>التاريخ والوقت<input type="datetime-local" name="startsAt" required/></label><label>السعة<input type="number" min="1" name="capacity" required/></label><label>رابط الفورم<input type="url" name="formUrl" required placeholder="https://..."/></label><label>القسم<select name="departmentId"><option value="">عام · كل الأقسام</option>{departments.map(d=><option key={d.id} value={d.id}>{d.nameAr}</option>)}</select></label><button className="primary-btn">نشر النشاط</button></form><div className="data-list">{activities.map(a=><div className="data-row" key={a.id}><div><strong>{a.title}</strong><div>{a.department?.nameAr||"عام"}</div></div><form action={deleteActivity}><input type="hidden" name="id" value={a.id}/><button className="danger-btn">حذف</button></form></div>)}</div></div><div className="admin-card" id="members"><h2>إنشاء حساب عضو</h2><p>لا توجد صفحة تسجيل ذاتي للأعضاء. الحساب يُنشأ من هنا فقط.</p><form action={createMember} className="stack-form"><label>الاسم<input name="name" required/></label><label>البريد<input name="email" type="email" required/></label><label>كلمة مرور مؤقتة<input name="password" type="password" minLength={8} required/></label><label>المسمى داخل النادي<input name="position" placeholder="مندوب هندسة الحاسوب"/></label><label>القسم<select name="departmentId"><option value="">بدون قسم</option>{departments.map(d=><option key={d.id} value={d.id}>{d.nameAr}</option>)}</select></label><button className="primary-btn">إنشاء حساب العضو</button></form><div className="data-list">{members.map(m=><div className="data-row" key={m.id}><div><strong>{m.name}</strong><div>{m.position||"عضو"} · {m.department?.nameAr||"بدون قسم"}</div></div></div>)}</div></div><div className="admin-card" id="structure"><h2>إضافة عنصر للهيكلية</h2><form action={addStructureItem} className="stack-form"><label>الاسم<input name="name" required/></label><label>المسمى<input name="title" required placeholder="رئيس النادي / مندوب..."/></label><label>المستوى التنظيمي<input type="number" name="level" defaultValue="1" min="1"/></label><label>ترتيب داخل المستوى<input type="number" name="sortOrder" defaultValue="0"/></label><label>القسم<select name="departmentId"><option value="">بدون قسم</option>{departments.map(d=><option key={d.id} value={d.id}>{d.nameAr}</option>)}</select></label><button className="primary-btn">إضافة للهيكلية</button></form></div><div className="admin-card" id="guides"><h2>تحرير دليل قسم</h2><p>اختر قسمًا ثم عدّل الحقول. تم تركها فارغة افتراضيًا كما طلبت.</p>{departments.map(d=><details key={d.id} style={{marginTop:12}}><summary style={{cursor:'pointer',fontFamily:'Alexandria'}}>{d.nameAr}</summary><form action={saveGuide} className="stack-form"><input type="hidden" name="departmentId" value={d.id}/><label>نبذة<textarea name="overview" defaultValue={d.guide?.overview}/></label><label>لمن يناسب<textarea name="fitFor" defaultValue={d.guide?.fitFor}/></label><label>مجالات العمل والعائد التقريبي<textarea name="careersIncome" defaultValue={d.guide?.careersIncome}/></label><label>الدورات والمهارات<textarea name="skillsCourses" defaultValue={d.guide?.skillsCourses}/></label><label>الفروقات مع تخصصات قريبة<textarea name="comparisons" defaultValue={d.guide?.comparisons}/></label><label>الأسئلة الشائعة<textarea name="faq" defaultValue={d.guide?.faq}/></label><button className="primary-btn">حفظ دليل القسم</button></form></details>)}</div></div></section></div>}
